import { Steps } from 'nextra/components'

# GTM

<Steps>
### Create a new GTM container for the brand.
### Export the "Combined Curiosity" GTM container and import it into the new container.
### Update the variables in the new GTM container as needed.
- Inside the "Folders" section of the new GTM container, find the `CONSTANTS_NOT_YET_SET` directory.
- Fill out each item within `CONSTANTS_NOT_YET_SET`.
- Move each completed item to a folder named `SET_CONSTANTS` as they are filled in.
</Steps>