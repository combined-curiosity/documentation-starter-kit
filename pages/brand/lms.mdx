import { Steps } from 'nextra/components'

# LMS

After brand and brandConfiguration have been added to the db:
<Steps>
  ### Update `index.html` with GTM token and abbreviation.
  ### Upload brand logo and favicon to S3 and add their links to `helpers/brandConfig`.
  ### Update `helpers/brandConfig` with abbreviation and other hardcoded strings.
</Steps>