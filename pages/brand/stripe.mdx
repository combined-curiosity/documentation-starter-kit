import { Steps } from 'nextra/components'

# Stripe

<Steps>
### Create a new Webhook going to `https://prd-xms-api.combinedcuriosity.io/api/stripe/webhooks/BRAND_ABBREVIATION/version/2/`.
  - Enable all events for the webhook.
  - Choose the latest version of the API.
### Create a test webhook going to `https://stg-xms-api.combinedcuriosity.io/api/stripe/webhooks/BRAND_ABBREVIATION/version/2/`.
  - Enable all events for the webhook.
  - Choose the latest version of the API.
</Steps>